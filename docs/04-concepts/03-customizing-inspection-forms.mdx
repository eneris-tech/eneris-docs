import ImageCustomizingInspectionForms1 from './img/customizing-inspection-forms-1.webp';
import ImageCustomizingInspectionForms2 from './img/customizing-inspection-forms-2.webp';
import ImageCustomizingInspectionForms3 from './img/customizing-inspection-forms-3.webp';
import ImageCustomizingInspectionForms4 from './img/customizing-inspection-forms-4.webp';
import ImageCustomizingInspectionForms5 from './img/customizing-inspection-forms-5.webp';
import ImageCustomizingInspectionForms6 from './img/customizing-inspection-forms-6.webp';
import ImageCustomizingInspectionForms7 from './img/customizing-inspection-forms-7.webp';
import ImageCustomizingInspectionForms8 from './img/customizing-inspection-forms-8.webp';

# Customizing Inspection Forms

You can customize **Inspection Forms** [link to [inspection forms](https://www.notion.so/Inspection-Forms-22cd9f0aeb5a80f9906ce9df5b4cea45?pvs=21)] in two ways:

1. Through **Form Templates** [link to [form templates](https://www.notion.so/Form-Templates-226d9f0aeb5a80ae8feed89bd3ebe833?pvs=21)]
2. Ad-hoc per **Inspection Form** [link to [inspection forms](https://www.notion.so/Inspection-Forms-22cd9f0aeb5a80f9906ce9df5b4cea45?pvs=21)]

## Customize Through Form Templates

Set up reusable **Form Templates** [link to [form templates](https://www.notion.so/Form-Templates-226d9f0aeb5a80ae8feed89bd3ebe833?pvs=21)] that include the exact customizations you need for any type of inspection.

- **Pre-selected entries** – Select disclaimers, limitations, and any other field option that should be automatically applied every time you use a particular **Form Template**.

:::tip

Once created, **Form Templates** can be reused across multiple inspections, saving time and improving report consistency.

:::

## Ad-Hoc Customizations Per Inspection Form

Make one-off changes directly in an individual **Inspection Form** [link to [inspection forms](https://www.notion.so/Inspection-Forms-22cd9f0aeb5a80f9906ce9df5b4cea45?pvs=21)].

- e.g., The property you are inspecting has an outdoor swimming pool. Even though a swimming pool is not part of your **Form Template**, you can easily add an `Outdoor Swimming Pool` component to your `Exterior` category if you need to inspect it.

## Types of customizations

Customizations include:

- **Custom categories** – Group components however you want.
- **Custom components** – Add new inspection items or rename existing ones.
- **Custom fields** – Create new fields [link to [field types](https://www.notion.so/Customizing-Inspection-Forms-24dd9f0aeb5a802fa284eac48dd9f673?pvs=21)].
- **Custom field options** – Add or edit the field options.
- **Required/Optional fields** – Choose which fields must be filled out
- **Visibility** – Hide fields from the final report
- **Edit field names** – Rename fields to suit your wording
- **Edit field options** – Change placeholder or default text for clarity
- **Edit disclaimers and limitations** – Change placeholder or default text for clarity

You have full control over how your **Inspection Forms** look and behave—without needing to code.

:::info

See the **Inspection Form** framework [link to [here](https://www.notion.so/Inspection-Forms-22cd9f0aeb5a80f9906ce9df5b4cea45?pvs=21)] documentation for a detailed explanation on categories, components, fields, and field options.

:::

## Creating new categories

To create new categories:

1. Select the edit icon in the `Inspection categories` section.
2. Scroll to the bottom of the page.
3. Enter a `category name`.
4. Select a `category icon`.
5. Select `create new category`.

<img src={ImageCustomizingInspectionForms1} style={{ maxWidth: '250px', width: '100%' }} alt="Creating new categories." />

### Editing categories

To edit existing categories:

1. Select the edit icon in the `Inspection categories` section.
2. Select the edit icon next to the category you would like to edit.
3. Edit the `category name` and `category icon` as desired.
4. Select `Save`.

<img src={ImageCustomizingInspectionForms2} style={{ maxWidth: '250px', width: '100%' }} alt="Editing categories." />

## Creating new components

To create new components:

1. Enter the category in which the new component should be created.
2. Select the edit icon near the top of the component list.
3. Enter the new component name.
4. Select `Create`.
5. The new component has been successfully created and can be located at the bottom of the component list.

<img src={ImageCustomizingInspectionForms3} style={{ maxWidth: '250px', width: '100%' }} alt="Creating new components." />

### Editing components

To edit existing components:

1. Enter the component you would like to edit.
2. Update the component name at the top of the page.

<img src={ImageCustomizingInspectionForms4} style={{ maxWidth: '250px', width: '100%' }} alt="Editing components." />

## Creating new fields

To create new fields:

1. Enter the component where the new field should be created.
2. Select the edit icon in the `Component fields` section.
3. Enter a new `field name`.
4. Select the `field type` [link to [field types](https://www.notion.so/Customizing-Inspection-Forms-24dd9f0aeb5a802fa284eac48dd9f673?pvs=21)]
5. Select `create`.
6. The new field has been successfully created and can be located at the bottom of the field list.

<img src={ImageCustomizingInspectionForms5} style={{ maxWidth: '250px', width: '100%' }} alt="Creating new fields." />

### Field types

You can use different types of fields in your **Inspection Forms**:

- **Checkbox** – Creates checkboxes for you to select one or more options.
- **Choice** – Creates radio buttons for you to select a single option.
- **Text** – For short text descriptions (50 characters max).
- **Multi-line Text** – For longer text descriptions (1,000 characters max).
- Media – Attach photos/videos.

Each field type lets you capture different types of data for better clarity in your reports.

:::info

You can create many fields of the same field type in an individual component.

:::

### Editing fields

To edit existing fields:

1. Enter the component where the field should be edited.
2. Select the edit icon next to the field that should be edited.
3. Edit the `field name`, `field requirement`, `field visibility`, `favorite options` and `option library`, as desired.

<img src={ImageCustomizingInspectionForms6} style={{ maxWidth: '250px', width: '100%' }} alt="Editing fields." />

:::info

Follow these links to field requirement [link to [here](https://www.notion.so/Customizing-Inspection-Forms-24dd9f0aeb5a802fa284eac48dd9f673?pvs=21)], field visibility [link to [here](https://www.notion.so/Customizing-Inspection-Forms-24dd9f0aeb5a802fa284eac48dd9f673?pvs=21)], favorite options [link to [here](https://www.notion.so/Customizing-Inspection-Forms-24dd9f0aeb5a802fa284eac48dd9f673?pvs=21)], and option library [link to [here](https://www.notion.so/Customizing-Inspection-Forms-24dd9f0aeb5a802fa284eac48dd9f673?pvs=21)] for more details.

:::

## Creating new field options

To create new field options:

1. Enter the component where you would like to create new field options.
2. All fields will have a `Add New`, `Add new choice`, `Add new text`, or `Add new checkbox` option. Selecting them will allow you to add a new field option.
    - Alternatively, you could select the edit icon next to the field and in that menu you will also be able to create new field options.

<img src={ImageCustomizingInspectionForms7} style={{ maxWidth: '250px', width: '100%' }} alt="Creating new field options." />

### Editing field options

To edit existing field options:

1. Enter the component where you would like to edit field options.
2. Select the edit icon next to the field which contains the field options that should be edited.
3. Review and edit the field options.

<img src={ImageCustomizingInspectionForms8} style={{ maxWidth: '250px', width: '100%' }} alt="Editing field options." />

## Additional field settings

In addition to the field name and field type there are a number of settings to help you customize your **Inspection Form**.

### Field requirement

A field can be toggled as `required` or `optional`.

- **Required** — When selected, the component list page will provide a visual indicator stating `Missing required fields`. This helps you quickly assess whether any component inspection was missed.
    - When all required fields within a component are completed the visual indicator will automatically update to `Completed fields`.
- **Optional** — When selected, the field is optional in your inspection.
    - By default, all fields are set as optional.

The field requirement toggle helps ensure required components and their fields are completed in a consistent manner.

:::info

In templates created by Eneris, the fields that are required are based on the relevant association’s Standards of Practice.

:::

### Field visibility

A field can be toggled as `show` or `hide`.

- **Show** — When selected, the field **will** appear in the final report.
    - By default, all fields are set as show.
- **Hide** — When selected, the field **will not** appear in the final report. The entries into this field are **only visible within the Inspection** [link to [inspection](https://www.notion.so/Inspections-226d9f0aeb5a8068b23fe5f466d0b8a8?pvs=21)].
    - This is helpful for internal notes, personal reminders, or communication between team members when inspecting a specific component.
    - e.g., Your **Form Template** [link to [form templates](https://www.notion.so/Form-Templates-226d9f0aeb5a80ae8feed89bd3ebe833?pvs=21)] includes two hidden fields in the Garage Doors component. The first is a checklist field to confirm whether the garage door is closed after inspection. The second is a media field for you to take photo proof the garage door is closed. **Neither of these fields will appear in the final report as they are hidden**.

### Favorite options

Both **text** and **multi-line text** field types allow you to select up to 10 favorite field options from the option library [link to [option library](https://www.notion.so/Customizing-Inspection-Forms-24dd9f0aeb5a802fa284eac48dd9f673?pvs=21)].

- **Text fields** — When a field option is selected as a favorite, it will appear next to the `Add New` button for easy selection when inspecting the component.
    - Add favorites by selecting the `+` icon next to the field option in the option library.
- **Multi-line text fields** — When a field option is selected as a favorite, it will appear below the text box when you select `Add new text` in the field entry.
    - Add favorites by swiping left and selecting the `+ Add` icon next tot he field option in the option library.

### Option library

A list of all saved responses for the field.

## Category limitations

In addition to general limitations [link to [general limitations](https://www.notion.so/Inspection-Forms-22cd9f0aeb5a80f9906ce9df5b4cea45?pvs=21)], you may also add **category limitations**.

- After entering a category, the top menu bar will have an option for `Limitations`. Enter any category-specific inspection limitations here.
    - e.g., If you are inspecting a roof and part of it is covered in solar panels you may wish to enter that limitation here.

## Category disclaimer

Inform your client(s) about category-specific disclaimers from your inspection.

- After entering a category, the top menu bar will have an option for `Disclaimer`. Enter any category-specific inspection disclaimer here.
    - e.g., The Standards of Practice you created, or follow, may have specific disclaimers you should provide to your client(s).